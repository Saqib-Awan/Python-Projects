**Print Colored Text**

This code snippet creates a function to display colored text in a Jupyter Notebook using HTML. It allows you to customize both the text color and background color by passing them as arguments. The final output is displayed using the IPython HTML function to render the colored text directly in the notebook.
